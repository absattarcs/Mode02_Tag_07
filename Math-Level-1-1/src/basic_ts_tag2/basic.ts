// Display Math.PI in the console
console.log("Math.PI:", Math.PI);

// PI with the appropriate value
const PI: number = Math.PI;

// Round the value of PI to two decimal places
const roundedPI: number = parseFloat(PI.toFixed(2));

//rounded value of PI in the console
console.log("Rounded PI:", roundedPI);
